#std -- import std

-- simple formula
@f(x: int): int := 2 x

-- f(x) should always be even
?double_is_even(x: int) := f(x) % 2

--* OUTPUT:

question to solve: ?double_is_even
	theory: `f(x) % 2`
	meaning:
		`f(x)`
			is divisible by
		`2`
			with
		x: int
	approach:
		1: substitute @f
			`f(x)` -> `2 x`
		2: rewrite using std::!simpl_mod
			`2 .. % 2` -> `.. % 1`
		lhs is divisible by rhs! (std::&mod_one)
	answer: correct (true)
	steps tried: 2079
	
1/1 answers are true
	
re-run with '--at=QUESTION:STEP' to see the
state of a question at the given step

*--
--* OUTPUT WITH --at=double_is_even:2 : 

question to solve: ?double_is_even
	theory: `f(x) % 2`
	step 2:
		state before step:
			`2 x % 2`
		step: rewrite using std::!simpl_mod
			`2 .. % 2` -> `.. % 1`
		state after step:
			`x % 1`

*--